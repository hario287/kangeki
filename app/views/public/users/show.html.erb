<!DOCTYPE html>
<div class='container'>
  <div class='row'>
    <% if @user.is_deleted == true %>
      <div class="col-12 col-sm-10 col-lg-8 col-xl-6 m-4">
        <h4>このユーザーは退会しています。</h4>
          <%= link_to :back do %>
        <div class="col-12 pt-4 back-link">前のページに戻る
        </div>
      </div>
      <% end %>
    <% else %>
    <h4 class="col-md-12 mt-3 pt-3">
      <i class="fas fa-user dark_blue"></i>
      <strong><%= @user.name %>さんのマイページ</strong></h4>
     <%= render 'info', user: @user %>
     <span class="border-bottom" style=""></span>
  </div>
</div>
<div class='container p-10'>
  <div class='row'>
    <div class="col-md-12 pt-3">
      <h4><i class="fas fa-pen-square dark_blue"></i>
      <strong>投稿したレビュー</strong></h4>
      <%= render 'public/reviews/index', reviews: @reviews %></p>
    </div>

    <div class='col-md-12 pt-3'>
      <h4><i class="fas fa-comment dark_blue"></i>
        <strong>談話室に投稿した記事</strong></h4>
      <%= render 'public/posts/index', posts: @posts %>
    </div>
  </div>
</div>
<% end %>