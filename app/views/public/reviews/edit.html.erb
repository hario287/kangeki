<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>
    	<%= render 'layouts/errors', obj: @review %>
  		<%= render 'form', review: @review %>
  		<%= link_to 'Show', @review, class: "book_#{@review.id}" %> |
  		<%= link_to 'Back', reviews_path, class: "back" %>
  </div>
</div>