<!DOCTYPE html>
<div class="container">
  <div class="row">
    <h3 class="col-md-12">
      <strong><%= @user.name %>さんのユーザー情報詳細</strong></h3>
  </div>
    <table class="table table-hover">
      <tr><%= image_tag @user.get_profile_image(120,120), class: "rounded-circle" %></tr>
      <tr>
        <td>ユーザー番号</td>
        <td>(<%= @user.id %>)</td>
      </tr>
      <tr>
        <td>ユーザーネーム</td>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <td>自己紹介</td>
        <td><%= @user.introduction %></td>
      </tr>
      <tr>
        <td>メールアドレス</td>
        <td><%= @user.email %></td>
      </tr>
      <tr>
        <td>ユーザーステータス</td>
        <td colspan='2'>
          <% if @user.is_deleted == true %>
          <div class="text-secondary font-weight-bold">退会</div>
          <% else %>
          <div class="text-success font-weight-bold">有効</div>
          <% end %>
          </td>
      </tr>
      <tr>
        <td>フォロー数</td>
        <td><%= @user.followings.count %></td>
      </tr>
      <tr>
        <td>フォロワー数</td>
        <td><%= @user.followers.count %></td>
      </tr>
    </table>

    <%= link_to "ユーザー情報を編集する", edit_admin_user_path,class: "btn btn-info" %>
  </div>
<div class='container mt-4'>
  <div class='row'>
    <div class="col-md-12 pt-3">
      <h4><strong><%= @user.name %>さんが投稿したレビュー</strong></h4>
      <%= render 'admin/user_reviews/list', reviews: @reviews %></p>
    </div>

    <div class='col-md-12 pt-3'>
      <h4><strong><%= @user.name %>さんが談話室に投稿した記事</strong></h4>
      <%= render 'admin/user_posts/list', posts: @posts %>
    </div>
  </div>
</div>